using UnityEngine;

public enum PopupGameType
{
    Fishing,
    Racing,
    Reflecting,
    Quizzing,
    Beating
}

public class PopupGameManager : MonoBehaviour
{

    [SerializeField] private GameObject popupGamePrefab;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
